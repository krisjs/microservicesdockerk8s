server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/krisjs/spring-app-config
          default-label: main
          skipSslValidation: true
          clone-on-start: true